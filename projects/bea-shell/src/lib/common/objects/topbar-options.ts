import { MenuItem } from '@bea-shell/common/objects';

export class TopbarOptions {
    height?: string
    width?: string
    logo?: MenuItem
    menuItems?: MenuItem[]
    disabled?: boolean
}