import { MenuIcon, MenuItem } from '@bea-shell/common/objects';

export class SidebarOptions {
    header?: MenuItem
    footer?: MenuItem
    menuItems?: MenuItem[]
    menuIcon?: MenuIcon
    disabled?: boolean
}